package constants

const (
	HTTPPort   = "HTTP_PORT"
	DBPort     = "DB_PORT"
	DBHost     = "DB_HOST"
	DBUser     = "DB_USER"
	DBPassword = "DB_PASSWORD"
	DBName     = "DB_NAME"
	APIHost    = "API_HOST"
)

const (
	BEARER                      = "Bearer"
	INVALID_TOKEN_ERROR_MESSAGE = "Invalid token."
)
